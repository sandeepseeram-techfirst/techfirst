when we run the package phase, Maven will automatically run validate, compile, test, and then package. 
If you run package, Maven will generate a JAR for you by default:

mvn package 



$ java -cp target/books-1.0-SNAPSHOT.jar com.oreilly.books.App
Hello World!
$ 